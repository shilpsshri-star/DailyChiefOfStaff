// Deprecated: storage moved from Vercel KV to Supabase (Postgres). See
// lib/db.ts (Profile/DailyLog/WeeklyReview/user-meta queries), lib/stats.ts
// (per-user stats jsonb), and lib/supabase.ts (client). Nothing in this app
// imports this file anymore -- kept only because this sandbox couldn't
// delete the file outright; safe to delete by hand in your repo.
export {};
